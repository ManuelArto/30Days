import 'package:flutter/material.dart';

class InsertScreen extends StatelessWidget {
  static const routeName = "/insertscreen";

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}